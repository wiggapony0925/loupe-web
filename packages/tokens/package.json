{
  "name": "@loupe/tokens",
  "version": "0.1.0",
  "description": "Single source of truth for the Loupe 'Precision' design tokens (colors, radii, spacing, type, motion). Pure data + an SCSS generator — no React, DOM, or RN. loupe-web's tokens.scss is generated from here; the Expo app builds its NativeWind vars from the same values.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.js": "./src/tokens.js"
  },
  "scripts": {
    "gen:scss": "node scripts/gen-scss.mjs",
    "typecheck": "tsc --noEmit"
  }
}
