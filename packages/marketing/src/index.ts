/** @loupe/marketing — shared pre-auth copy + trending-feed shaping (web + app). */
export { HERO, FEATURES, NOT_FOUND, type MarketingFeature } from "./copy";
export { pickPopulated, interleaveById } from "./feed";
